# このファイルを .env にコピーして値を入れてください: cp .env.example .env
# 値はクォート不要。= の前後にスペースを入れないこと。

# Discord Botのトークン（Developer Portal > Bot > Reset Token で取得）
DISCORD_BOT_TOKEN=ここにトークン

# 目覚ましの音を鳴らすボイスチャンネルのID（右クリック > IDをコピー）
DISCORD_CHANNEL_ID=0

# 設定（時刻・スヌーズ）を保存しておくテキストチャンネルのID
CONFIG_CHANNEL_ID=0
